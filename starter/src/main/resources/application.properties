# ===============================
# = DATA SOURCE
# ===============================

# Connection url for the database "iris"
spring.datasource.jdbcUrl = jdbc:mysql://178.252.79.250:13306/iris?useOldAliasMetadataBehavior=true&zeroDateTimeBehavior=convertToNull
spring.datasource.driverClassName = com.mysql.jdbc.Driver
spring.datasource.maximumPoolSize = 5

# Username and password
spring.datasource.username = root
spring.datasource.password = secaccess

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

logging.level.ru.iris = DEBUG

# ===============================
# = IRIS
# ===============================

iris.config.zwaveDebug = true
iris.config.openzwaveCfgPath = /home/nix
iris.config.zwavePort = /dev/ttyUSB0

# Available modules - (yandex OR google), zwave, noolite, weather, web, ai
spring.profiles.active = yandex

# Yandex API KEY
iris.config.yandexApiKey = 5a849502-dc73-40ba-9676-f9aa3b34d4ca
iris.config.yandexLanguage = ru-RU
# (jane, omazh, zahar, ermil)
iris.config.yandexVoice = ermil